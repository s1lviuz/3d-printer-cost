interface Printer {
    id: number
    name: string
    wattage: number
}

interface Filament {
    id: number
    name: string
    color: string
    material: string
    cost: number
}

interface RegionCost {
    id: number
    name: string
    kwhCost: number
}